---
- hosts: localhost
  connection: local
  vars_files:
    - './vars/common.yml'

  roles:
    - role: 'apt_sources'
    - role: 'common'
    - role: 'rtorrent'
    - role: 'sshd'
    - role: 'docker'
      docker_username: 'limed'
      docker_group: 'limed
    - role: 'git'
      git_user: 'limed'
      git_email: 'limed@sudoers.org'
      github_username: 'limed'
      github_name: 'Ed Lim'
