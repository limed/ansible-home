---
- name: 'Install apache2'
  apt:
    name: 'apache2'
    update_cache: 'yes'
    state: latest

- name: 'Enable mod_rewrite'
  apache2_module:
    name: 'rewrite'
    state: 'present'
  notify:
    - 'restart apache2'

- name: 'Enable mod_ssl'
  apache2_module:
    name: 'ssl'
    state: 'present'
  notify:
    - 'restart apache2'
